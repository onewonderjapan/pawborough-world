# Shared / machine-local artifacts — never commit
node_modules
node_modules/
.zcode/
dist/
.cache/
*.log
logs/
*.pid

# Task-local test sandboxes (created per-run by validator/negative tests)
sandbox*/
tmp/
.night-runner-state

# OS / editor noise
.DS_Store
Thumbs.db
__pycache__/
*.pyc

# C4: dist-compressed/ is a build output — reproduce with tools/build_compressed_dist.mjs
# (its sources: committed world/**/ *.cm.glb + *.cm.json + the dist/ baseline)
dist-compressed/
# player-experience batch A: verification build output (fresh outDir per run)
dist-player-exp-a/

# candidate dist build outputs (rebuildable via scripts/build_lane_a_polish_dist.mjs)
dist-*/
# Blender save backups (*.blend stays tracked as the reopenable source)
*.blend1

# world-playable-night restore-verify dir (kept on disk, not committed)
restore-world-playable/

# world-playable-night: the ZIP stays on disk, digest receipt is committed
artifacts/world-playable/delivery/*.zip

# world-ten-hour 20260921: same convention — restore-verify dirs and ZIPs stay
# on disk (kept, never deleted), digest receipts and smoke JSONs are committed
restore-world-ten-hour-20260921*/
artifacts/world-ten-hour/round-003/delivery/*.zip
artifacts/world-ten-hour/round-005/delivery/*.zip

# Migrated source stays in Git; runtime/generated assets use SHA manifests and private S3.
/scene-authoring/yuyuan-area/out/
/scene-authoring/yuyuan-area/out-*/
/scene-authoring/yuyuan-area/resources/
/scene-authoring/yuyuan-area/.python-deps/
# M1: fixed staging dir for pipeline inputs (garden-kit site modules) — binaries via
# docs/MIGRATION-ASSETS.json, never in the repo.
/scene-authoring/yuyuan-area/staged/
/scene-authoring/yuyuan-area/baseline/*.glb
/scene-authoring/yuyuan-area/inputs/*.glb
/asset-authoring/**/props/
/asset-authoring/**/textures/
/asset-authoring/**/*.glb
/asset-authoring/**/*.blend
/asset-authoring/**/shots/
/asset-authoring/**/out/
/.local-media/
/scene-authoring/yuyuan-area/review-images/

# Review screenshots / renders stay out of the repo (owner decision 2026-09-24: the repo is public).
# Keep them in the work-order package under /home/baibai/outbox/<package>/artifacts/ instead.
/artifacts/**/*.png
/artifacts/**/*.jpg
/artifacts/**/*.jpeg
/artifacts/**/*.webp
/scene-authoring/**/review/*.png
/scene-authoring/**/review/*.jpg
/scene-authoring/**/renders/*.png
/scene-authoring/**/renders/*.jpg
