# 豫园入口门楼候选 v2 — 主控三项补修（2026-09-21 21:34 审查 → 21:58 交付）

目录 `workspace-v2/`（v1 `workspace/` 原样保留）。`ownerAdopted=false`，未接世界。
`model.glb` 16,368 tris / 3,399,552 bytes（v1 15,808 / 3,331,596）；sha256 `0251ea21bb18673dd411d83c8537d0f995b9c4e4864bbd515b29066538f368dc`；`ground.glb` 同 v1 方式独立。

## 修了什么（对应审查 1/2/3）
1. **檐下层叠**：叠涩改为逐层实接——柱脚 0.30 坐在顶枋上，之后 宽板0.09/窄垫0.07 交替 3 层，每层与下层满面接触，最上一层垫块高度算到檐枋底（by1）为止；无 0.015 空带。叠涩背后补砖砌**檐下墙**（H+0.2 → 檐枋底），檐口下方封板降到檐枋底同高，正面不再透天。（build.py `bracket()`、`frieze-wall`、`eave-soffit`）
2. **屋面翘角/檐边收敛**：翘角抬升 1.35→0.85 m、外挑 0.45→0.28、影响段 1.7→2.1 m、衰减指数 2.4→2.0（角部过渡更连续），嫩戗由 3 段减为 2 段且弧度减半；檐口凹沉 0.07→0.05；瓦当加宽 0.105→0.118 并在檐口加连续滴水条，椽只从檐枋外露出。含翘角总宽 11.73→11.13 m，翘角顶 7.61 m，全资产最高点 8.26 m（正脊两端饰块）——measurements 现按几何实测写 `cornerTipTopActual` 与 `maxYActual`，不再写公式估算。面数级别不变（+560 tris，来自滴水条与檐下墙）。
3. **门洞净空**：正面门套区开口抬到 3.32 m，两级雀替落在 y 3.00–3.24（净空矩形外），门洞通道仍 2.2×3.0，衬石与 3.0 天花之间加石过渡块；门楣高度改为算到回纹带底，不再压入回纹带。检查脚本取消例外：2.2×3.0 整矩形内 0 顶点 / 0 三角形；沿 −Z 射线样点 x0/y1.8、x0/y2.9、x±0.95/y2.65、**x±0.95/y2.9**、x±1.05/y2.98、x±1.05/y0.2 全部贯通（`check-report.json`）。

## 同机位图
`shots/front-pbr.jpg`、`shots/left-3q-pbr.jpg`、`shots/eave-close-pbr.jpg`（Cycles CPU 4 线程 64 spp，同 v1 相机与灯光；世界下半球略提亮 .62→.72 以看清檐下）。檐下近景里叠涩之间檐枋下方仍有深影区（砖砌檐下墙在檐枋+封板之下的自然阴影），几何上已封闭（封板 y 6.25–6.32 与叠涩顶、檐枋底同高）。

## 其他
背面闭合 888 采样 0 漏空；法线 32,059 单位长无 NaN；贴图 1,747,547 bytes 10 张。未做：Validator、世界安装、长测、压缩、打包。
预览页同 v1（`./preview/serve.sh` → 5445，未启动）。
本轮耗时约 24 分钟（含 3 次重建/渲染迭代，每次生成 ≈5 s，三张图 ≈3 min）。
